const initialState = {
    username: '',
    password: '',
    profile_pic: 'https://robohas.org/animal.png'
}

export default function reducer(state=initialState, action){
    switch(action.type){
        default:
        return state
    }
}